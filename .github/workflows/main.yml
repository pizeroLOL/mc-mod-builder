name: mod-build
on:
  push
jobs:
  clone:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
  build:
    runs-on: ubuntu-20.04
    steps: 
    - run:
          sh ./mc-1.12.2-auto/start.sh
    needs: clone
